{
  "version": "2.8.0",
  "license": "MIT",
  "scripts": {
    "preview": "python3 -m http.server 8080 --directory src",
    "deploy:azure": "swa deploy ./src --env production --deployment-token <DEPLOYMENT-TOKEN>",
    "deploy:github-pages": "echo \"GitHub Pages deploys automatically from .github/workflows/github-pages.yml\""
  }
}
